# Core dependencies for SVG Welder
toml>=0.10.2
requests>=2.28.0

# Optional validation dependencies
# Install with: pip install svg-welder[validation]
lxml>=5.0.0
xmlschema>=2.0.0
gcodeparser>=0.2.0
pygcode>=0.2.0
